require("CreateTableStatus")

function Server_StartDistribution(game, standing)
    CTS(game,standing)
end         -- If players pick their starting postions this starts and creates the Table for the Status of the various Relations